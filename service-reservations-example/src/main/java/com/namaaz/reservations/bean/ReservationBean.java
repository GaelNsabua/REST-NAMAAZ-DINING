package com.namaaz.reservations.bean;

import jakarta.enterprise.context.RequestScoped;
import jakarta.inject.Named;

@Named
@RequestScoped
public class ReservationBean {
    // DTO / adaptateur pour UI
}
