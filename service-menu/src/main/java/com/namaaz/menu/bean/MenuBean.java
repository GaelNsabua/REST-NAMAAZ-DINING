package com.namaaz.menu.bean;

import jakarta.enterprise.context.RequestScoped;
import jakarta.inject.Named;

@Named
@RequestScoped
public class MenuBean {
    // DTO / adaptateur pour JSF si nécessaire
}
